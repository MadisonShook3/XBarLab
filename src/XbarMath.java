import java.util.Collection;

/**
 * Does some math
 */
public class XbarMath {

  //start with ANOVA to see if the iterations are in control
  public boolean anovaTest(Collection<double[]> categoryData, double alpha) {

    return false;
  }
}
